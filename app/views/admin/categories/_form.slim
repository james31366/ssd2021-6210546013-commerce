= form_with model: [:admin, category] do |f|
  div = f.text_field :name, placeholder: 'Name'
  div = f.text_area :description, placeholder: 'Write content here'
  div = f.submit 'Submit Category', class: 'btn btn-outline-primary'